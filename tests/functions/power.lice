
function power(x: number, n: number): number {
    if (n == 0) { return 1; }
    if ((x % 2) == 0) {
        return power(x * x, n / 2);
    } else {
        return power(x, n - 1);
    }
}

function main(): void {
    let number x = 2;
    let number n = 2;

    let number p = power(x, n);

    return;
}

main();
