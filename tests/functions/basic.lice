
function main(): number {
    return 22;
}

let c = main();

function two(a: number): bool {
    let cond = (a + 1) * 0;
    if (((a + 1) * 0) == 23) {
        return true;
    } else return false;
}

let bool is_true = two(c - 1);
